# .gitignore

# Python specific
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
venv/
.env
.pytest_cache/
htmlcov/
.tox/
*.egg-info/
.mypy_cache/
.pytest_cache/
build/
dist/

# Solidity/EVM specific artifacts
# (If you had brownie/hardhat build folders, you'd include them)
# Since we're using solcx directly, it might not generate these by default,
# but if you add a build folder later, include it.
artifacts/
cache/

# OS generated files
.DS_Store
.idea/ # For PyCharm or IntelliJ users
.vscode/ # For VS Code users (optional, if you want to share settings, remove this)

# Local data generated by scripts
xrpl_did_data.json
